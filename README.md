# C-Frequency-Tracker
Purchase frequencies are stored in a std::map&lt;std::string, int>, a container that enforces alphabetical key ordering and eliminates the need for manual sorting. At startup, the program automatically generates a frequency.dat backup file containing all item counts without user intervention.
